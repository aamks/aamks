import { RampFilterPipe } from './ramp-filter.pipe';

describe('RampFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RampFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
