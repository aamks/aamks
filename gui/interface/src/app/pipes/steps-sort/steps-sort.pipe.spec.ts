import { StepsSortPipe } from './steps-sort.pipe';

describe('StepsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StepsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
