import { StringInputDirective } from './string-input.directive';

describe('StringInputDirective', () => {
  it('should create an instance', () => {
    const directive = new StringInputDirective();
    expect(directive).toBeTruthy();
  });
});
